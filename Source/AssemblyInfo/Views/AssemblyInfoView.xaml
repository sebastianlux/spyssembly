<UserControl x:Class="AssemblyInfo.Views.AssemblyInfoView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             mc:Ignorable="d" 
             d:DesignHeight="300" d:DesignWidth="300">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
        </Grid.RowDefinitions>
        <TextBlock Grid.Row="0" 
                   Text="PRODUCT VERSION" />
        <TextBlock Grid.Row="1" Text="{Binding ProductVersion}" />
        <TextBlock Grid.Row="2"
                   Text="FILE VERSION" />
        <TextBlock Grid.Row="3" Text="{Binding FileVersion}" />
        <TextBlock Grid.Row="4"
                   Text="INFORMATIONAL VERSION" />
        <TextBlock Grid.Row="5" Text="{Binding InformationalVersion}" />
    </Grid>
</UserControl>
